## 1. Tux authoring and protocol foundation

- [x] 1.1 Add the compact palette/target/gem map schema and deterministic Bun compiler to canonical `LevelSpec v1` JSON.
- [x] 1.2 Add compiler validation for dimensions, active-mask equality, symbols, stable IDs, canonical ordering, 450–550 active cells, 70%–80% initial matches, Shelf capacity, and per-color conservation.
- [x] 1.3 Extend TypeScript, native C++, WASM, canonical JSON, and differential fixtures with `obsidian`, `pearl`, and `amber` in one clean protocol cutover.
- [x] 1.4 Generalize Shelf state/validation from the fixed twelve-slot baseline to configured compact capacity while preserving partial extraction, append, rejection, compaction, and victory semantics.

## 2. Flagship Tux fixture

- [x] 2.1 Author the exact 24×32 Tux target map with 450–550 active sockets and the approved six-color subset.
- [x] 2.2 Author a color-conserving initial gem map with 70%–80% locked cells and intentional eight-neighbor movable components.
- [x] 2.3 Compile and load `tux-01` identically in the TypeScript oracle, native C++, WASM, Harness, and production App while retaining `prism-01` only as a focused regression fixture.
- [x] 2.4 Produce and commit a complete `tux-01.win.json` trace that reaches `Won` with empty sixteen-slot Shelf across all three backends.

## 3. Dense pixel assets and renderer

- [x] 3.1 Create and inspect a board Micro socket/gem master geometry suitable for the minimum portrait scale.
- [x] 3.2 Extend semantic palette manifests and derive/review/promote Micro `obsidian`, `pearl`, `amber`, `navy`, `ice`, and `coral` variants with matching Alpha masks.
- [x] 3.3 Render only active Tux coordinates as target/socket/gem layers, keep locked gems flush at full clarity, and expose target rims beneath raised mismatches.
- [x] 3.4 Render the one logical sixteen-slot Shelf as ordered eight-slot banks with large tray/gem assets and no enclosing dashboard panels.

## 4. Adaptive stage and authoritative motion

- [x] 4.1 Implement dynamic viewport/safe-area measurement and deterministic side-bank versus top/bottom-bank candidate scoring at integer board-cell scales.
- [x] 4.2 Center the complete Tux stage in desktop, square, and portrait viewports with no page overflow, top-pinned flow, or logical coordinate changes.
- [x] 4.3 Add bounded integer-step board zoom/pan when fitted portrait cells fall below the direct-touch threshold, including keyboard/assistive semantics and camera reset on viewport changes.
- [x] 4.4 Replace React ghost timing with a single WAAPI/FLIP animation coordinator that captures pre/post rectangles, hides destinations, animates one clone, and cleans up on finish/cancel/fallback.
- [x] 4.5 Keep Shelf-survivor FLIP separate from moved-gem flight, release input after authoritative animation settlement, and preserve immediate reduced-motion state.
- [x] 4.6 Add the wordless solved-Tux center-out shimmer and leave the terminal image visible without replay, progression, or reset controls.

## 5. Working proof and cleanup

- [x] 5.1 Smoke-play the compiled Tux fixture through selection, Shelf storage, target placement, compaction, zoom/pan, rejection, and victory in the real browser before adding cleanup coverage.
- [x] 5.2 Verify flight frames show exactly one moved gem, source/end alignment within one CSS pixel, no page-origin excursion, no fading trail, and no early input unlock.
- [x] 5.3 Add focused compiler, palette, configured-Shelf, Tux replay, motion, responsive-layout, accessibility, reduced-motion, native, WASM, and differential regression tests after the smoke path works.
- [x] 5.4 Run `pixel-bloom` review, native/WASM parity, Bun checks, browser E2E at desktop/square/390px portrait, production build, and strict OpenSpec validation.
- [ ] 5.5 Update maintained architecture/fixture documentation and archive this change only after human Tux visual and gameplay approval.
